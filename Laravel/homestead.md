> Download **vagrant** and **virtualbox** and intall

`vagrant box add laravel/homestead`

`composer global require "laravel/homestead=~2.0"`

`cd ~/.composer`

> add PATH
`sudo vi ~/.zshrc`

`homestead init`

